@model CarMeetFinder.Models.CarDetail
@{
    ViewBag.Title = "Details";
}

<link href="https://fonts.googleapis.com/css?family=Exo" rel="stylesheet">


<h2 id="indexHeader">Details</h2>

<div>
    <hr />
    <div class="card">
        <h1 id="headerOne">
            @Html.DisplayFor(model => model.Make)
            <br />
            @Html.DisplayFor(model => model.VehicleModel)
            <br />
            @Html.DisplayFor(model => model.DisplayName)
            <br />
            @Html.DisplayFor(model => model.Specifications)
            <br />
            @Html.DisplayFor(model => model.Description)
        </h1>
    </div>
    <hr />
</div>

<p>
    @Html.ActionLink("Edit", "Edit", null, new { id = Model.CarID }, new { @class = "actionlinkbacktolist" }) |
    @Html.ActionLink("Back to List", "Index", null, new { @class = "actionlinkbacktolist" }) |
    @Html.ActionLink("Delete", "Delete", null, new { id = Model.CarID }, new { @class = "actionlinkbacktolist" })

</p>
